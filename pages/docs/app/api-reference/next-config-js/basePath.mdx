---
title: basePath
description: Use `basePath` to deploy a Next.js application under a sub-path of a domain.
---

{/* The content of this doc is shared between the app and pages router. You can use the `<PagesOnly>Content</PagesOnly>` component to add content that is specific to the Pages Router. Any shared content should not be wrapped in a component. */}

# basePath

Next.js 애플리케이션을 도메인의 하위 경로에 배포하려면 `basePath` 구성 옵션을 사용할 수 있습니다.

`basePath`를 사용하면 애플리케이션에 대한 경로 접두사를 설정할 수 있습니다. 예를 들어, 기본값인 `''`(빈 문자열) 대신 `/docs`를 사용하려면 `next.config.js`를 열고 `basePath` 구성을 추가하세요:

```js filename="next.config.js"
module.exports = {
  basePath: "/docs",
};
```

> **Good to know**: 이 값은 빌드 시에 설정해야 하며 클라이언트 측 번들에 인라인으로 포함되기 때문에 다시 빌드하지 않고는 변경할 수 없습니다.

### 링크

`next/link` 및 `next/router`를 사용하여 다른 페이지에 링크할 때 `basePath`가 자동으로 적용됩니다.

예를 들어, `/about`을 사용하면 `basePath`가 `/docs`로 설정된 경우 자동으로 `/docs/about`가 됩니다.

```js
export default function HomePage() {
  return (
    <>
      <Link href="/about">About Page</Link>
    </>
  );
}
```

출력 HTML:

```html
<a href="/docs/about">About Page</a>
```

이렇게 하면 `basePath` 값을 변경할 때 애플리케이션의 모든 링크를 변경할 필요가 없습니다.

### 이미지

<AppOnly>

[`next/image`](/docs/app/api-reference/components/image) 컴포넌트를 사용할 때 `src` 앞에 `basePath`를 추가해야 합니다.

</AppOnly>

<PagesOnly>

[`next/image`](/docs/pages/api-reference/components/image) 컴포넌트를 사용할 때 `src` 앞에 `basePath`를 추가해야 합니다.

</PagesOnly>

예를 들어, `/docs/me.png`를 사용하면 `basePath`가 `/docs`로 설정된 경우 이미지가 제대로 제공됩니다.

```jsx
import Image from "next/image";

function Home() {
  return (
    <>
      <h1>My Homepage</h1>
      <Image
        src="/docs/me.png"
        alt="Picture of the author"
        width={500}
        height={500}
      />
      <p>Welcome to my homepage!</p>
    </>
  );
}

export default Home;
```
